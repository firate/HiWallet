{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*"

  // Topup:WebhookUrl ve Topup:WebhookSecret .env / User Secrets'tan gelir, buraya
  // YAZILMAZ (baseline.md madde 1). Secret özellikle: topup-webhook'taki
  // Providers:stripe-fake:WebhookSecret ile aynı olmak zorunda, ayrışırsa her
  // webhook 401 alır.
}
